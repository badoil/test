#pragma once
#include <string>
#include <vector>

// Simple whitespace tokenizer
// input: const std::string& s
// output: std::vector<std::string> tokens
std::vector<std::string> tokenize(const std::string& s);
